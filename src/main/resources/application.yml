spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Security-db?createDatabaseIfNotExist=true&userSSL=true&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDateTimeCode=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: localhost
    port: 25

server:
  port: 8080
